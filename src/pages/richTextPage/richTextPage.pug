view.page
  - var title = "wxParse"
  - var desc = "富文本解析"
  include ../../templates/pageHead.pug

  view.page__bd.page__bd_spacing
    wxParse(
      nodes="{{htmlCode}}"
      language="html"
    )  

    view.mt15
      wxParse(
        nodes="{{doc}}"
        language="html"
      )
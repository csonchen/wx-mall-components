view.page
  - var title = "grid"
  - var desc = "格子布局"
  include ../../templates/pageHead.pug

  view
    grid(list="{{grids}}")

  view.page__bd.page__bd_spacing
    view.mt15
      wxParse(nodes="{{doc}}")